# L3 빠른 결정 기록 — 2026년 7월

구현 수준(L3) 결정 모음. 별도 ADR을 만들 정도는 아니나 근거 보존을 위해 기록한다.

## Quick Decision Log — 2026-07-03

| 항목 | 내용 |
| --- | --- |
| **결정** | Supabase 테이블에 `rag_` 네임스페이스 접두어 사용(`rag_documents`, `rag_chunks`) |
| **이유** | 프로젝트에 이전 실습 테이블(`documents` 59행, `documents_test` 53행)이 존재. 내가 만들지 않은 데이터라 삭제 없이 충돌 회피 |
| **대안** | 기존 테이블 재사용(구조 불일치), 기존 삭제(비파괴 원칙 위배) |
| **위험** | 이름이 길어짐 — 수용. 기존 테이블 RLS 비활성화는 사용자 정리 항목으로 별도 안내 |
| **작성자** | 한재진 |

## Quick Decision Log — 2026-07-03

| 항목 | 내용 |
| --- | --- |
| **결정** | GeminiProvider의 thinking 기본 비활성화(thinking_budget=0) |
| **이유** | gemini-2.5-flash가 사고에 출력 토큰을 소진해 심판 JSON이 잘림 → faithfulness 오판정. RAG 답변/심판/라우팅은 직접 출력이 필요 |
| **대안** | max_tokens 상향(근본 해결 아님), 다른 모델 사용 |
| **위험** | 복잡 추론이 필요한 질문에서 품질 저하 가능 — 규정 QA에선 영향 경미, 필요 시 인자로 활성화 |
| **작성자** | 한재진 |

## Quick Decision Log — 2026-07-03

| 항목 | 내용 |
| --- | --- |
| **결정** | HNSW 벡터 인덱스 + 한국어 tsvector는 `simple` 설정 |
| **이유** | HNSW: 학습 목적·문서 증가 대비(현 94청크에선 완전탐색과 차이 미미, 레포트 명시). tsvector simple: PostgreSQL에 한국어 형태소 분석기 기본 부재 |
| **대안** | IVFFlat(재색인 필요), pg_bigm(무료티어 확장 제약) |
| **위험** | simple은 조사 변형('신고를/신고는')을 다른 토큰 처리 → 하이브리드에서 벡터가 보완. Phase 7에서 벡터-only vs 하이브리드 비교 예정 |
| **작성자** | 한재진 |

## Quick Decision Log — 2026-07-03

| 항목 | 내용 |
| --- | --- |
| **결정** | 재업로드 정책: 같은 파일명은 삭제 후 재적재(교체), `source_filename` 유니크 |
| **이유** | 업로드 탭에서 동일 문서 갱신 시 중복 방지. cascade로 기존 청크 정리, `replaced` 플래그로 사용자에게 표시 |
| **대안** | 버전 누적(용량↑·검색 혼선), 업로드 거부(사용성↓) |
| **위험** | 파일명 동일·내용 다른 경우도 교체 — content_hash로 감지 가능하게 저장 |
| **작성자** | 한재진 |

## Quick Decision Log — 2026-07-03

| 항목 | 내용 |
| --- | --- |
| **결정** | 임베딩 토큰 계수기 의존성 주입(기본 글자수÷2 휴리스틱) |
| **이유** | 청킹 분할 판단에 토큰 수 필요하나 tiktoken 의존을 피하고 테스트 결정성 확보. 정확 토큰은 임베딩 단계 API로 확정 |
| **대안** | tiktoken 직접 사용(의존성·모델별 상이) |
| **위험** | 한국어는 글자당 토큰이 더 조밀 → 추정 오차. 분할 임계 판단엔 충분 |
| **작성자** | 한재진 |
